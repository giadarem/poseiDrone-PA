export const TOKEN_RULES = {
  MIN_REQUIRED_FOR_PLAN: 5, // minimo per poter creare un piano
  COST_PER_PLAN: 2,         // token addebitati per ogni piano valido
  DEFAULT_PLAN_COST: 5,     // costo "richiesta" nel piano (campo requestCost nel DB)
};